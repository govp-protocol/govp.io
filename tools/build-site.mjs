import { readFile, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
const version = '0.1.10';
const sourceCommit = '84a096836c409a0aa45e7511d80f6e4c871091ca';
const releaseUrl = 'https://github.com/govp-protocol/govp/releases/tag/v0.1.10';
const pypiUrl = 'https://pypi.org/project/govp/0.1.10/';
const assetRevision = '0.1.10-20260805.1';
const asset = (path) => `${path}?v=${assetRevision}`;
const socialPreviewUrl = 'https://govp.io/govp/assets/govp-social-preview.png';

function socialMeta(title, description) {
  return `<meta name="theme-color" content="#07182C"><meta name="color-scheme" content="light"><meta property="og:type" content="website"><meta property="og:site_name" content="GOVP"><meta property="og:title" content="${title}"><meta property="og:description" content="${description}"><meta property="og:image" content="${socialPreviewUrl}"><meta property="og:image:width" content="1280"><meta property="og:image:height" content="640"><meta property="og:image:alt" content="GOVP — Sign once. Verify anywhere."><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${title}"><meta name="twitter:description" content="${description}"><meta name="twitter:image" content="${socialPreviewUrl}">`;
}

function escapeHtml(value) {
  return value
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;');
}

function header(active = '') {
  const links = [
    ['install', '/govp/download.html', 'Install'],
    ['docs', '/govp/docs.html', 'Documentation'],
    ['spec', '/govp/spec/govp-1.html', 'Specification'],
    ['verify', '/govp/verify.html', 'Verify'],
    ['conformance', '/govp/conformance.html', 'Conformance'],
  ];
  return `<header class="site-header">
    <div class="wrap nav">
      <a class="brand" href="/" aria-label="GOVP home"><img class="brand-mark" src="${asset('/govp/assets/govp-mark.svg')}" alt=""><span class="brand-name">GOVP</span></a>
      <nav class="nav-links" aria-label="Primary">${links.map(([key, href, label]) => `<a${active === key ? ' class="active" aria-current="page"' : ''} href="${href}">${label}</a>`).join('')}</nav>
      <div class="nav-right"><a class="btn btn--ghost btn--sm" href="https://github.com/govp-protocol/govp">Source</a><button class="icon-btn nav-toggle" type="button" data-nav-toggle aria-label="Open navigation" aria-expanded="false">Menu</button></div>
    </div>
    <nav class="mobile-menu" data-mobile-menu aria-label="Mobile">${links.map(([, href, label]) => `<a href="${href}">${label}</a>`).join('')}<a href="https://github.com/govp-protocol/govp">Source</a></nav>
  </header>`;
}

function footer() {
  return `<footer class="site-footer"><div class="wrap">
    <div class="footer-grid">
      <div class="footer-about"><a class="brand" href="/"><img class="brand-mark" src="${asset('/govp/assets/govp-mark.svg')}" alt=""><span class="brand-name">GOVP</span></a><p>Open protocol for independently verifiable digital artifacts.</p></div>
      <div class="footer-col"><h4>Protocol</h4><a href="/govp/spec/govp-1.html">GOVP-1</a><a href="/govp/status.html">Status &amp; rotation</a><a href="/govp/conformance.html">Conformance</a><a href="/govp/schemas/govp-1.schema.json">JSON Schema</a></div>
      <div class="footer-col"><h4>Use</h4><a href="/govp/download.html">Install GOVP</a><a href="/govp/quickstart.html">Quick start</a><a href="/govp/integration.html">Integration</a><a href="/govp/verify.html">Browser verifier</a></div>
      <div class="footer-col"><h4>Project</h4><a href="/govp/governance.html">Governance</a><a href="/govp/security.html">Security</a><a href="https://github.com/govp-protocol/govp">GitHub</a></div>
    </div>
    <div class="footer-base"><p>GOVP ${version} · Apache-2.0 · Copyright Brilyetz Holding S.L. · Gemacode is a brand of Brilyetz Holding S.L.</p><div class="footer-legal"><a href="https://github.com/govp-protocol/govp/blob/main/LICENSE">License</a><a href="https://github.com/govp-protocol/govp/blob/main/TRADEMARKS.md">Trademarks</a><a href="mailto:research@gemacode.org">Contact</a></div></div>
  </div></footer>`;
}

function page({ title, description, active = '', eyebrow = `GOVP ${version}`, body, lang = 'en', extraHead = '', module = '' }) {
  return `<!doctype html>
<html lang="${lang}" data-theme="light"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} · GOVP</title><meta name="description" content="${description}">${socialMeta(`${title} · GOVP`, description)}<link rel="icon" href="${asset('/favicon.svg')}"><link rel="stylesheet" href="${asset('/govp/assets/govp.css')}">${extraHead}</head>
<body>${header(active)}<main><section class="section section--tight grid-bg"><div class="wrap"><div class="measure stack-16"><span class="eyebrow">${eyebrow}</span><h1 class="h1">${title}</h1><p class="lead">${description}</p></div></div></section><hr class="section-divider"><section class="section section--tight"><div class="wrap docs-layout"><article class="prose">${body}</article></div></section></main>${footer()}<script src="${asset('/govp/assets/govp.js')}"></script>${module ? `<script type="module" src="${module}"></script>` : ''}</body></html>\n`;
}

const pages = new Map();

pages.set('index.html', `<!doctype html>
<html lang="en" data-theme="light"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>GOVP · Sign once. Verify anywhere.</title><meta name="description" content="GOVP is an open protocol for independently verifiable digital artifacts.">${socialMeta('GOVP — Sign once. Verify anywhere.', 'Open protocol for independently verifiable digital artifacts.')}<link rel="icon" href="${asset('/favicon.svg')}"><link rel="stylesheet" href="${asset('/govp/assets/govp.css')}"></head>
<body>${header('')}<main><section class="section grid-bg"><div class="wrap hero-grid"><div class="hero-copy stack-24"><span class="tag tag--ok"><span class="dot"></span>GOVP-1 stable · verifier ${version}</span><h1 class="display">Sign once. Verify anywhere.</h1><p class="lead">GOVP places a readable Ed25519-signed record beside any digital artifact. Recipients verify the signature and exact bytes locally, without an account, a central API or a proprietary verifier.</p><div class="flex-gap"><a class="btn btn--primary btn--lg" href="/govp/quickstart.html">Start with GOVP</a><a class="btn btn--ghost btn--lg" href="/govp/spec/govp-1.html">Read the specification</a></div><p class="steward-line">Open protocol · <strong>stewarded by Gemacode</strong></p></div><div class="hero-proof" aria-label="Example of local GOVP verification"><div class="proof-header"><img src="${asset('/govp/assets/govp-mark.svg')}" alt=""><span>LOCAL VERIFICATION</span></div><div class="proof-stack"><div class="proof-file"><strong>record.govp.txt</strong><code>Version: GOVP-1<br>Signature: Ed25519<br>Asset-SHA256: 2e6870bc…</code></div><div class="proof-link">bound to exact bytes</div><div class="proof-file"><strong>artifact.bin</strong><code>SHA-256: 2e6870bc…</code></div></div><div class="proof-result"><span>VALID</span><small>format · signature · govp-id · asset</small></div></div></div></section>
<hr class="section-divider"><section class="section section--tight"><div class="wrap"><div class="flow-intro stack-16"><span class="eyebrow">Portable verification</span><h2 class="h2">A record that keeps working after the artifact travels</h2><p class="lead">The publisher signs once. The record and artifact can be mirrored, archived or delivered by any channel. Verification remains local and implementation-independent.</p></div><img class="flow-figure" src="${asset('/govp/assets/govp-flow.svg')}" alt="Sign a portable GOVP record, distribute it beside an artifact, then verify both locally."><ol class="flow-mobile" aria-label="GOVP verification flow"><li><span>01 · Sign</span><strong>Publisher</strong><p>Signs a readable GOVP record for the exact artifact bytes.</p></li><li><span>02 · Distribute</span><strong>Record + artifact</strong><p>Travel through mirrors, archives, email or independent systems.</p></li><li><span>03 · Verify</span><strong>Any recipient</strong><p>Checks signature and integrity locally, with no GOVP account.</p></li></ol></div></section>
<hr class="section-divider"><section class="section"><div class="wrap"><div class="grid-3"><div class="card"><span class="eyebrow eyebrow--plain">Portable</span><h2 class="h3">A self-contained record</h2><p class="text-2">The public key travels with the record. No GOVP account or central service is required.</p></div><div class="card"><span class="eyebrow eyebrow--plain">Deterministic</span><h2 class="h3">Byte-exact verification</h2><p class="text-2">The frozen signing input and conformance vectors make implementations comparable across runtimes.</p></div><div class="card"><span class="eyebrow eyebrow--plain">Bounded</span><h2 class="h3">Cryptographic evidence</h2><p class="text-2">GOVP proves signature and integrity. It does not claim that the signed statement is true.</p></div></div></div></section>
<hr class="section-divider"><section class="section"><div class="wrap"><div class="grid-2"><div class="stack-16"><span class="eyebrow">Composable by design</span><h2 class="h2">Keep the evidence stack you already trust</h2><p class="lead">GOVP can bind SCITT receipts, COSE messages, DSSE envelopes, in-toto attestations and Sigstore bundles as exact upstream evidence objects. Their native verifiers still decide their internal validity; GOVP keeps the verified bytes portable.</p><a class="arrow-link" href="/govp/composition.html">See the layered verification model →</a></div><div class="card"><h3 class="h3">Separate, cumulative verdicts</h3><p class="text-2"><strong>GOVP valid</strong> confirms the GOVP record and exact byte binding. <strong>Upstream valid</strong> confirms the source signature, receipt, attestation or transparency proof. Application policy decides whether both are sufficient.</p></div></div></div></section>
<hr class="section-divider"><section class="section"><div class="wrap"><div class="grid-2"><div class="stack-16"><span class="eyebrow">Public foundation</span><h2 class="h2">One protocol, one normative source</h2><p class="lead">The specification, schema, vectors, reference verifier and synthetic example are published together under Apache-2.0.</p><a class="arrow-link" href="/govp/docs.html">Explore the documentation →</a></div><div class="card code-card"><pre><code># Install the stable verifier
python -m pip install govp==${version}
govp self-test
govp conformance --run
govp verify-url https://govp.io/.well-known/govp.txt</code></pre></div></div></div></section></main>${footer()}<script src="${asset('/govp/assets/govp.js')}"></script></body></html>\n`);

pages.set('es/index.html', page({
  lang: 'es', title: 'Evidencia que permanece verificable', description: 'GOVP es un protocolo abierto para evidencia operativa portable y verificable de forma independiente.',
  body: `<h2>Qué publica GOVP</h2><p>Un registro GOVP-1 enlaza el SHA-256 de un artefacto, una identidad editorial canónica y una firma Ed25519. El registro se verifica localmente, sin cuentas ni una API central.</p><div class="grid-3"><div class="card"><h3>Portable</h3><p>La clave pública viaja dentro del registro.</p></div><div class="card"><h3>Determinista</h3><p>La entrada de firma y los vectores fijan el comportamiento exacto.</p></div><div class="card"><h3>Limitado</h3><p>Prueba firma e integridad, no la veracidad del contenido firmado.</p></div></div><h2>Se integra con la evidencia existente</h2><p>GOVP puede vincular como bytes exactos declaraciones o recibos SCITT, mensajes COSE, sobres DSSE, attestations in-toto y bundles Sigstore. No sustituye sus verificadores: GOVP comprueba su propio registro y la integridad del objeto; cada ecosistema valida sus firmas, identidades, recibos y políticas.</p><p><a href="/govp/composition.html">Ver el modelo de composición por capas</a></p><h2>Versión estable</h2><p>GOVP-1 es el formato congelado. El verificador ${version} está disponible públicamente en PyPI e incluye especificación, esquema, vectores de conformidad y ejemplos sintéticos.</p><pre><code>python -m pip install govp==${version}
govp conformance --run</code></pre><p><a class="btn btn--primary" href="/govp/quickstart.html">Comenzar</a> <a class="btn btn--ghost" href="/govp/spec/govp-1.html">Especificación normativa</a></p><p>La documentación normativa se mantiene en inglés para conservar una única interpretación canónica.</p>`,
}));

pages.set('404.html', page({ title: 'Page not found', description: 'The requested GOVP resource does not exist.', body: '<p><a class="btn btn--primary" href="/">Return to GOVP</a></p>' }));

pages.set('govp/docs.html', page({
  active: 'docs', title: 'Documentation', description: 'The complete route from protocol overview to independent implementation.',
  body: `<div class="grid-3"><a class="card card--hover" href="/govp/quickstart.html"><h2>Quick start</h2><p>Install from PyPI and validate a record.</p></a><a class="card card--hover" href="/govp/protocol.html"><h2>Protocol model</h2><p>Understand records, signing input and identifiers.</p></a><a class="card card--hover" href="/govp/integration.html"><h2>Integration &amp; issuance</h2><p>Verify and issue through stable Python and CLI contracts.</p></a><a class="card card--hover" href="/govp/status.html"><h2>Status &amp; rotation</h2><p>Evaluate live key authorization and record revocation.</p></a><a class="card card--hover" href="/govp/composition.html"><h2>Composition</h2><p>Combine GOVP with signing, attestation and transparency ecosystems.</p></a><a class="card card--hover" href="/govp/conformance.html"><h2>Conformance</h2><p>Implement against byte-exact fixtures.</p></a><a class="card card--hover" href="/govp/security.html"><h2>Security</h2><p>Threat model, limits and reporting.</p></a><a class="card card--hover" href="/govp/governance.html"><h2>Governance</h2><p>How the frozen format and registry evolve.</p></a></div><h2>Normative and machine-readable material</h2><ul><li><a href="/govp/spec/govp-1.html">GOVP-1 specification</a> and <a href="/govp/spec/govp-1.txt">plain text</a></li><li><a href="/govp/status.html">GOVP-STATUS-1</a>, <a href="/govp/schemas/govp-status-1.schema.json">status schema</a> and <a href="/govp/conformance/status-vectors.json">status vectors</a></li><li><a href="/govp/schemas/govp-1.schema.json">GOVP-1 JSON Schema</a></li><li><a href="/govp/conformance/vectors.json">text conformance vectors</a> and <a href="/govp/conformance/json-vectors.json">JSON vectors</a></li><li><a href="/PROTOCOL-SOURCE.json">publication provenance manifest</a></li><li><a href="${releaseUrl}">signed, checksummed release ${version}</a></li></ul><div class="callout"><strong>Conflict rule.</strong> The normative text explains the format; the published conformance vectors decide exact implementation behavior.</div>`,
}));

pages.set('govp/protocol.html', page({
  active: 'spec', title: 'Protocol model', description: 'How GOVP-1 turns an artifact digest and publisher declaration into portable signed evidence.',
  body: `<h2>Record model</h2><p>A GOVP-1 record is UTF-8 text. It declares the protocol version, canonical publisher URI, publisher, asset type and identifier, SHA-256 digest, evidence URI, public key, signature and content-derived GOVP-ID.</p><ol><li>The issuer hashes the exact artifact bytes with SHA-256.</li><li>All non-empty fields except <code>signature</code> are canonicalized into a byte-exact signing input.</li><li>The issuer signs that input with Ed25519.</li><li>A verifier reconstructs the same bytes, verifies the signature and recomputes the GOVP-ID and optional asset digest.</li></ol><h2>Offline core</h2><p>Core verification requires only the record and, for the asset check, the asset bytes. Remote verification is an additional transport check: the final HTTPS URL must equal the signed <code>canonical</code> URI.</p><h2>What it establishes</h2><ul><li>Control of the private key corresponding to the embedded public key.</li><li>Integrity of the signed record fields.</li><li>Identity of the declared asset through SHA-256 when bytes are supplied.</li></ul><h2>What it does not establish</h2><ul><li>The truth, legality or quality of a signed claim.</li><li>Independent trust in the publisher.</li><li>An independently anchored time unless a separate profile provides one.</li></ul><p><a class="btn btn--primary" href="/govp/spec/govp-1.html">Read the normative specification</a></p>`,
}));

pages.set('govp/status.html', page({
  active: 'docs', title: 'Status, revocation and key rotation', description: 'GOVP-STATUS-1 adds a fail-closed, same-origin HTTPS decision about active keys and revoked records without changing GOVP-1.',
  body: `<div class="callout"><strong>Separate result.</strong> Core validity remains an offline cryptographic fact. <code>currently_trusted</code> is produced only after live canonical HTTPS retrieval of both record and status.</div><h2>Public endpoint</h2><pre><code>https://govp.io/.well-known/govp/revoked.json</code></pre><p>The discovery <a href="/.well-known/govp/index.json">index</a> references the status endpoint. Its response is public, CORS-enabled and sent with <code>Cache-Control: no-store</code>.</p><h2>Evaluate it</h2><pre><code>govp status-url https://govp.io/.well-known/govp.txt \\
  --status-url https://govp.io/.well-known/govp/revoked.json --json</code></pre><p>A true result requires a core-valid canonically fetched record, a valid status document from the same origin, an active matching key and no explicit record revocation. Offline status evaluation returns a snapshot verdict and never claims current trust.</p><h2>Machine-readable contract</h2><ul><li><a href="/govp/schemas/govp-status-1.schema.json">GOVP-STATUS-1 JSON Schema</a></li><li><a href="/govp/conformance/status-vectors.json">Status conformance vectors</a></li><li><a href="https://github.com/govp-protocol/govp/blob/main/extensions/status-1/GOVP-STATUS-1.md">Normative extension</a></li><li><a href="https://github.com/govp-protocol/govp/blob/main/docs/key-lifecycle.md">Rotation and compromise runbook</a></li></ul><h2>Trust boundary</h2><p>The authority is the current HTTPS origin, not the record signing key: a compromised signing key cannot safely revoke itself. Saved status files are historical snapshots. Deployments that need a stronger independent anchor can add DNSSEC, transparency, PKI or an external status signer.</p>`,
}));

pages.set('govp/quickstart.html', page({
  active: 'docs', title: 'Quick start', description: 'Install GOVP from PyPI, run the complete conformance suite and verify a bundled synthetic example.',
  body: `<h2>Requirements</h2><p>Use Python 3.10 or newer. Install the stable verifier from PyPI:</p><pre><code>python -m pip install govp==${version}
govp self-test
govp conformance --run
govp status-conformance --run</code></pre><h2>Extract and verify the bundled example</h2><pre><code>govp examples --extract govp-examples
govp verify govp-examples/manufacturing-record.govp.txt \\
  --asset govp-examples/manufacturing-record.statement.txt</code></pre><p>Exit code <code>0</code> means the requested verification succeeded, <code>1</code> means the record was evaluated and is invalid, and <code>2</code> means the command or input could not be processed.</p><h2>Verify canonical identity and live status</h2><pre><code>govp verify-url https://govp.io/.well-known/govp.txt --json
govp status-url https://govp.io/.well-known/govp.txt \\
  --status-url https://govp.io/.well-known/govp/revoked.json --json</code></pre><p>The first command verifies format, signature, GOVP-ID and the exact final canonical URL. The second separately requires a same-origin live status document, an active key and no record revocation. Use <a href="/PROTOCOL-SOURCE.json">PROTOCOL-SOURCE.json</a> and the release checksums for provenance.</p><h2>Machine-readable result</h2><pre><code>govp verify record.govp.txt --asset artifact.bin --json</code></pre><p>Consumers must inspect every check and retain advisory warnings. Do not infer success only from process output text.</p><h2>Browser verification</h2><p>The <a href="/govp/verify.html">browser verifier</a> uses the same frozen rules and does not upload the record or asset.</p>`,
}));

pages.set('govp/integration.html', page({
  active: 'docs', title: 'Integration guide', description: 'Stable interfaces and safety rules for applications consuming GOVP records.',
  body: `<h2>Python API</h2><pre><code>from pathlib import Path
from govp import load_record, verify

record = load_record("record.govp.txt")
result = verify(record, asset_bytes=Path("artifact.bin").read_bytes())
if not result.ok:
    raise ValueError(result.checks)</code></pre><p>The supported top-level API also exports <code>parse_record</code>, <code>derive_govp_id</code>, <code>signing_input</code>, <code>sign_record</code>, <code>serialize_record</code>, <code>evaluate_status</code>, <code>VerifyResult</code> and <code>StatusResult</code>. SemVer applies to these documented exports.</p><h2>Issue without reconstructing the signature format</h2><pre><code>govp issue --asset artifact.bin \\
  --canonical 'https://issuer.example/.well-known/govp/{govp-id}.govp' \\
  --publisher "Example issuer" --asset-type document \\
  --asset-id example/release --evidence https://issuer.example/release \\
  --private-key issuer-private.pem --output record.govp</code></pre><p>The issuer computes the digest, ID, raw public key and signature and refuses to overwrite existing output. Keep keys outside repositories and use an HSM/KMS for high-value production.</p><h2>CLI JSON contract</h2><pre><code>govp verify RECORD --asset FILE --json</code></pre><p>Treat the process exit code and each returned check as the interface. Preserve unknown fields, warnings and the original record for auditability.</p><h2>Verification policy</h2><table><thead><tr><th>Check</th><th>Meaning</th></tr></thead><tbody><tr><td><code>format</code></td><td>Required fields and encodings are valid.</td></tr><tr><td><code>signature</code></td><td>Ed25519 signature matches the deterministic signing input.</td></tr><tr><td><code>govp-id</code></td><td>Identifier matches type, asset ID and digest.</td></tr><tr><td><code>canonical</code></td><td>Final remote URL equals the signed canonical URI, when requested.</td></tr><tr><td><code>asset</code></td><td>Supplied asset bytes match the signed SHA-256.</td></tr></tbody></table><h2>Enterprise HTTPS</h2><p>Network commands use Certifi by default and accept an explicit <code>--ca-bundle PEM</code>. They do not silently inherit <code>SSL_CERT_FILE</code> or <code>REQUESTS_CA_BUNDLE</code>. Approved enterprise transport can download a record for offline verification; canonical and live-status checks then remain not evaluated.</p><h2>Composed evidence</h2><p>The asset can be an upstream evidence object such as a SCITT receipt, COSE message, DSSE envelope, in-toto attestation or Sigstore bundle. Require both the GOVP asset check and the upstream system's native verification, then apply local policy. Keep the verdicts separate: <code>govp_valid</code> does not imply <code>upstream_valid</code>.</p><p><a href="/govp/composition.html">Composition model and system-specific boundaries</a></p><h2>Untrusted input</h2><ul><li>Bound record and asset sizes before buffering.</li><li>Never execute or render evidence URLs as trusted markup.</li><li>Use HTTPS with normal certificate validation for remote retrieval.</li><li>Do not silently follow a redirect to a URL different from <code>canonical</code>.</li><li>Do not discard unknown signed fields.</li></ul><h2>Compatibility</h2><p>GOVP-1 is frozen. New semantics require a governed extension or a new protocol version; integrations must fail closed on an unknown <code>version</code>.</p>`,
}));

pages.set('govp/examples.html', page({
  active: 'docs', title: 'Synthetic example', description: 'A complete signed GOVP-1 record and its matching statement, containing no production identity or personal data.',
  body: `<h2>Files</h2><ul><li><a href="/govp/examples/manufacturing-record.govp.txt">manufacturing-record.govp.txt</a></li><li><a href="/govp/examples/manufacturing-record.statement.txt">manufacturing-record.statement.txt</a></li></ul><h2>Verify</h2><pre><code>govp verify manufacturing-record.govp.txt \\
  --asset manufacturing-record.statement.txt</code></pre><p>The example exists to exercise the public format. Its domains and identities are synthetic and do not represent an operational deployment.</p>`,
}));

pages.set('govp/conformance.html', page({
  active: 'conformance', title: 'Conformance', description: 'Byte-exact fixtures that decide whether an implementation conforms to GOVP-CORE-CONFORMANCE-1.',
  body: `<h2>Required fixtures</h2><ul><li><a href="/govp/conformance/vectors.json">Text record vectors</a>: parsing, signing bytes, IDs, signatures, URLs and timestamps.</li><li><a href="/govp/conformance/json-vectors.json">JSON vectors</a>: typed records, bundles, aliases and invalid Unicode.</li><li><a href="/govp/schemas/govp-1.schema.json">JSON Schema</a>: structural validation for machine-readable exchange.</li><li><a href="/govp/conformance/status-vectors.json">GOVP-STATUS-1 vectors</a> and <a href="/govp/schemas/govp-status-1.schema.json">status schema</a>: key and revocation state.</li></ul><h2>Implementation rule</h2><p>A conforming implementation must reproduce the expected parsing result, signing-input SHA-256, GOVP-ID, signature decision, canonical URL decision and warnings for every applicable fixture. Status implementations additionally reproduce the separate status suite.</p><div class="callout"><strong>Exactness matters.</strong> Runtime-default Unicode trimming, Unicode case folding, URL normalization or date parsing are not substitutes for the frozen GOVP-1 rules.</div><h2>Installed suites</h2><pre><code>govp conformance --run
govp status-conformance --run</code></pre><h2>Source suite</h2><pre><code>git clone https://github.com/govp-protocol/govp.git
cd govp
python -m pip install -r requirements-test.txt
python -m pytest</code></pre><p>The publication website also tests its independent browser engine against both vector sets.</p>`,
}));

pages.set('govp/security.html', page({
  active: 'docs', title: 'Security model', description: 'GOVP verifies cryptographic integrity inside explicit trust and transport boundaries.',
  body: `<h2>Threat model</h2><p>Records, assets, URLs, field names and field values are untrusted input. The verifier must reject malformed encoding, invalid key or signature sizes, invalid hashes, prohibited control characters, ambiguous timestamps and non-ASCII URI data.</p><h2>Trust boundary</h2><p>An embedded public key proves only that the matching private key signed the record. Applications decide separately whether that key, canonical domain or publisher is trusted. <a href="/govp/status.html">GOVP-STATUS-1</a> can provide the origin's current key and revocation decision without changing core validity.</p><h2>Remote verification</h2><p>Remote mode requires HTTPS, bounded reads and timeouts, TLS validation and exact final canonical URLs. Certifi is the default CA store; enterprise users opt in explicitly with <code>--ca-bundle PEM</code>. Ambient CA variables are deliberately ignored. Evidence URIs are data, not executable instructions.</p><h2>Privacy</h2><p>The browser verifier runs locally and does not upload records, assets or results. The protocol itself has no user account or telemetry requirement.</p><h2>Report a vulnerability</h2><p>Do not disclose an unpatched vulnerability in a public issue. Follow the private reporting instructions in the repository’s <a href="https://github.com/govp-protocol/govp/security/policy">security policy</a> or email <a href="mailto:research@gemacode.org">research@gemacode.org</a>.</p>`,
}));

pages.set('govp/governance.html', page({
  active: 'docs', title: 'Governance', description: 'A conservative change process for a portable format whose value depends on deterministic behavior.',
  body: `<h2>Frozen core</h2><p>GOVP-1 signing input, field semantics, identifier derivation and existing conformance vectors are frozen. A verifier patch may reject inputs that were always invalid, but it must not change the signing bytes of a valid GOVP-1 record.</p><h2>Change classes</h2><ul><li><strong>Editorial:</strong> clarification with no behavioral change.</li><li><strong>Registry:</strong> a reviewed addition such as an asset type code.</li><li><strong>Profile:</strong> additional rules layered on the core.</li><li><strong>Protocol:</strong> incompatible behavior requiring a new version.</li></ul><h2>Decision record</h2><p>Proposals and decisions are public in the canonical repository. Changes require tests, conformance impact analysis, security review and clear migration guidance. Brilyetz Holding S.L., operating through its Gemacode brand, is the current project steward.</p><p><a class="btn btn--ghost" href="https://github.com/govp-protocol/govp/blob/main/GOVERNANCE.md">Repository governance document</a></p>`,
}));

pages.set('govp/trust-model.html', page({
  title: 'Trust model', description: 'Separate cryptographic validity, publisher trust, transport identity and claim truth.',
  body: `<h2>Five separate questions</h2><ol><li><strong>Is the record structurally valid?</strong> Format, ID and encodings pass.</li><li><strong>Was it signed by the embedded key?</strong> The Ed25519 signature passes.</li><li><strong>Does the origin currently authorize the key and record?</strong> GOVP-STATUS-1 evaluates this separately.</li><li><strong>Do I trust that origin or publisher?</strong> This is application policy.</li><li><strong>Is the signed claim true?</strong> GOVP alone cannot answer this.</li></ol><p>Keeping these questions separate prevents a green cryptographic result from being presented as institutional endorsement or factual proof.</p><h2>Canonical identity</h2><p>The <code>canonical</code> HTTPS URI binds remote publication to the exact final record URL. It does not create a global certificate authority and it does not replace ordinary domain or organizational due diligence.</p>`,
}));

pages.set('govp/composition.html', page({
  title: 'Composing GOVP with evidence ecosystems', description: 'Use GOVP with signing, attestation and transparency systems while preserving every verification boundary.',
  body: `<div class="callout"><strong>Composition, not substitution.</strong> GOVP binds and signs the exact upstream evidence bytes. The upstream system's native verifier remains responsible for its internal signature, receipt, certificate, transparency proof, attestation semantics and policy.</div><h2>Three cumulative layers</h2><table><thead><tr><th>Layer</th><th>What it establishes</th></tr></thead><tbody><tr><td>GOVP core</td><td>GOVP record validity and exact binding to supplied asset bytes</td></tr><tr><td>Native verifier</td><td>The source system's signature, receipt, certificate, envelope or attestation validity</td></tr><tr><td>Application policy</td><td>Whether the verified identities, predicates, time and trust roots are acceptable</td></tr></tbody></table><pre><code>accepted = govp_valid AND upstream_valid AND policy_allows(result)</code></pre><h2>Composable evidence inputs</h2><table><thead><tr><th>Ecosystem</th><th>GOVP-bound object</th><th>Native verification that remains required</th></tr></thead><tbody><tr><td><a href="https://datatracker.ietf.org/doc/draft-ietf-scitt-architecture/">SCITT</a></td><td>Signed statement, receipt or retained bundle</td><td>Receipt, transparency service and SCITT policy</td></tr><tr><td><a href="https://www.rfc-editor.org/rfc/rfc9052.html">COSE</a></td><td>Exact serialized COSE message</td><td>Headers, algorithm, key, signature or MAC and policy</td></tr><tr><td><a href="https://github.com/secure-systems-lab/dsse">DSSE</a></td><td>Exact serialized DSSE envelope</td><td>Pre-authentication encoding, signatures, signer identity and payload type</td></tr><tr><td><a href="https://github.com/in-toto/attestation/blob/main/spec/README.md">in-toto</a></td><td>Attestation, envelope or bundle</td><td>Envelope, subject, predicate semantics and supply-chain policy</td></tr><tr><td><a href="https://docs.sigstore.dev/about/bundle/">Sigstore</a></td><td>Bundle or release evidence</td><td>Signature, certificate identity, Rekor evidence, trusted root and policy</td></tr><tr><td><a href="https://www.w3.org/TR/vc-data-model-2.0/">Verifiable Credentials</a></td><td>Exact credential or presentation representation</td><td>Securing mechanism, issuer, status, schema and relying-party policy</td></tr><tr><td><a href="https://spec.c2pa.org/specifications/">C2PA</a></td><td>C2PA object or media asset</td><td>Native C2PA validation, trust model and assertions</td></tr></tbody></table><p>COSE and DSSE are formats or envelopes; in-toto is an attestation framework; SCITT defines an architecture for transparency services; and Sigstore is a signing and transparency ecosystem.</p><h2>Integration sequence</h2><ol><li>Produce and natively verify the upstream evidence.</li><li>Preserve the exact byte sequence that passed verification.</li><li>Issue a GOVP record that binds those bytes through <code>asset-sha256</code>.</li><li>Distribute the GOVP record and upstream object together.</li><li>At receipt, run GOVP verification, native verification and application policy as distinct checks.</li></ol><p>The signed <code>evidence</code> URI can identify a location, but remains untrusted input. GOVP core does not automatically dereference it or validate the object found there.</p><h2>Bidirectional composition</h2><p>A GOVP record can itself be submitted to a transparency, attestation or signing system that accepts arbitrary payloads. For example, a deployment can register a GOVP record with a SCITT transparency service while retaining GOVP's offline verification properties.</p><h2>Current implementation boundary</h2><p>The GOVP ${version} reference verifier validates GOVP-1 and its binding to supplied bytes. It does not include native verifiers for the systems above. Applications invoke those implementations explicitly; future governed profiles can standardize orchestration without changing GOVP-1.</p><p><a class="btn btn--primary" href="https://github.com/govp-protocol/govp/blob/main/docs/composition.md">Read the complete composition guide</a></p>`,
}));

pages.set('govp/changelog.html', page({
  title: 'Changelog', description: 'Public protocol and verifier publication history.',
  body: `<h2>0.1.10 — status and issuance</h2><ul><li>Adds path-like API compatibility and explicit enterprise CA selection.</li><li>Publishes safe issuance APIs and <code>govp issue</code>.</li><li>Publishes GOVP-STATUS-1, live revocation, key rotation and status conformance.</li><li>Adds production endpoint monitoring and exact-canonical discovery records.</li></ul><p>GOVP-STATUS-1 is a separate extension. The frozen GOVP-1 signing bytes and identifiers remain unchanged.</p><h2>0.1.9 — stable public distribution</h2><ul><li>Publishes GOVP through <code>pip install govp</code> with bundled specification, schema, conformance vectors and synthetic examples.</li><li>Adds <code>govp conformance --run</code> and <code>govp examples --extract DIR</code>.</li><li>Defines the supported top-level Python API and keeps <code>govp.core</code> compatible.</li><li>Publishes checksummed standalone binaries for Linux, macOS and Windows.</li><li>Adds a live, signed GOVP record at <code>https://govp.io/.well-known/govp.txt</code>.</li></ul><h2>Provenance</h2><p>Release ${version} is built from signed tag <code>v${version}</code>, commit <code>${sourceCommit}</code>. Exact source and normative artifact hashes are recorded in <a href="/PROTOCOL-SOURCE.json">PROTOCOL-SOURCE.json</a>.</p><p><a href="${releaseUrl}">Release ${version} and SHA256SUMS</a> · <a href="https://github.com/govp-protocol/govp/blob/main/CHANGELOG.md">Full source changelog</a></p>`,
}));

pages.set('govp/download.html', page({
  active: 'install', title: 'Get GOVP', description: 'Install the stable Python package or download a checksummed standalone verifier.',
  body: `<h2>Install from PyPI</h2><pre><code>python -m pip install govp==${version}
govp self-test
govp conformance --run</code></pre><p><a class="btn btn--primary" href="${pypiUrl}">GOVP ${version} on PyPI</a></p><p>A convenient <a href="/install.sh">installation helper</a> installs the same pinned package after showing the command.</p><h2>Standalone executables</h2><p>The <a href="${releaseUrl}">GitHub release</a> provides checksummed binaries for Linux x86-64 and ARM64, macOS Intel and Apple silicon, and Windows x86-64. Verify every download against <code>SHA256SUMS</code>.</p><h2>Source and provenance</h2><p><a href="https://github.com/govp-protocol/govp">Canonical repository</a> · <a href="${releaseUrl}">Signed tag and release assets</a> · <a href="/PROTOCOL-SOURCE.json">Website provenance manifest</a></p><p>Release ${version}, its Python distributions, standalone binaries, license notices and checksums were published together from commit <code>${sourceCommit}</code>.</p>`,
}));

pages.set('govp/faq.html', page({
  title: 'Frequently asked questions', description: 'Practical answers about validity, identity, privacy and compatibility.',
  body: `<h2>Does verification require the GOVP website?</h2><p>No. Core verification is local. You need the record and, for the asset check, the asset bytes.</p><h2>Does a valid signature prove the statement is true?</h2><p>No. It proves that the matching private key signed the record. Trust and factual evaluation remain separate.</p><h2>Does GOVP replace SCITT, COSE, DSSE, in-toto or Sigstore?</h2><p>No. GOVP can bind their output as exact upstream evidence bytes. Their native verifiers still validate their signatures, receipts, certificates, attestations and policies.</p><h2>Does GOVP store user data?</h2><p>The protocol has no account or central storage requirement. This site’s browser verifier processes selected files locally.</p><h2>Can I add a custom field?</h2><p>Yes. Unknown non-empty fields are preserved and signed. Use stable, documented names and do not conflict with registered fields.</p><h2>Can I normalize a URL before comparing it?</h2><p>No. Remote canonical comparison is strict after the verifier’s limited syntax validation. Do not rewrite host, path, percent escapes, port or trailing slash.</p><h2>Is GOVP-1 stable?</h2><p>Yes. Incompatible behavior requires a future protocol version.</p>`,
}));

pages.set('govp/glossary.html', page({
  title: 'Glossary', description: 'Precise terms used by the GOVP-1 specification and verifier.',
  body: `<dl><dt>Asset</dt><dd>The exact bytes identified by the signed SHA-256 digest.</dd><dt>Canonical URI</dt><dd>The exact HTTPS publication identity signed into the record.</dd><dt>Core-valid</dt><dd>The record format, signature and GOVP-ID checks pass; asset and transport checks depend on supplied inputs.</dd><dt>Evidence URI</dt><dd>An issuer-declared location for the asset or statement. It remains untrusted data.</dd><dt>GOVP-ID</dt><dd>A content-derived identifier computed from asset type, asset ID and asset SHA-256.</dd><dt>Native verifier</dt><dd>The implementation that validates an upstream evidence object's own format, signatures and trust semantics.</dd><dt>Profile</dt><dd>Additional governed rules layered on the frozen core.</dd><dt>Signing input</dt><dd>The exact domain-separated UTF-8 bytes verified by Ed25519.</dd><dt>Upstream evidence object</dt><dd>An exact signed message, receipt, envelope, attestation, bundle or other object supplied as the GOVP asset.</dd><dt>Vector</dt><dd>A fixture with expected parsing, hashes, validation decisions and warnings.</dd></dl>`,
}));

pages.set('govp/use-cases.html', page({
  title: 'Use cases', description: 'Where a small portable integrity record is useful, with its limits kept visible.',
  body: `<div class="grid-3"><div class="card"><h2>Documents</h2><p>Bind a published statement or policy to exact bytes and a signing key.</p></div><div class="card"><h2>Datasets</h2><p>Identify a frozen dataset snapshot used for analysis or evaluation.</p></div><div class="card"><h2>Models</h2><p>Attach a signed record to model artifacts or cards.</p></div><div class="card"><h2>Pipelines</h2><p>Record the digest of a build output or release artifact.</p></div><div class="card"><h2>Benchmarks</h2><p>Bind a declared result set to its exact published bytes.</p></div><div class="card"><h2>Agents</h2><p>Publish an operating declaration without relying on a central GOVP account.</p></div></div><h2>Suitability test</h2><p>Use GOVP when recipients need a small, durable way to check who signed a record and whether supplied bytes match it. Add external systems when you also need identity certification, independent timestamps, revocation, availability or factual adjudication.</p>`,
}));

pages.set('govp/verify.html', page({
  active: 'verify', title: 'Browser verifier', description: 'Verify a GOVP-1 text record or JSON bundle locally. Selected data is not uploaded.',
  module: asset('/govp/assets/verify-ui.js'),
  body: `<div class="callout"><strong>Local processing.</strong> This page does not send the record, asset or result to GOVP. For automation and bounded remote retrieval, use the reference CLI.</div><form id="verify-form" class="verifier" novalidate><div class="field"><label for="record">GOVP record or JSON bundle</label><textarea id="record" rows="14" spellcheck="false" required></textarea></div><div class="field"><label for="canonical-url">Fetched canonical URL (optional exact transport check)</label><input id="canonical-url" type="url" inputmode="url" placeholder="https://publisher.example/.well-known/govp.txt"></div><div class="field"><label for="asset">Asset file (optional, maximum 16 MiB)</label><input id="asset" type="file"></div><div class="flex-gap"><button class="btn btn--primary" type="submit">Verify locally</button><button class="btn btn--ghost" id="load-example" type="button">Load synthetic example</button><button class="btn btn--ghost" type="reset">Clear</button></div></form><section id="verification-result" class="verification-result" aria-live="polite" hidden><h2>Result</h2><p id="result-summary" class="lead"></p><dl id="result-checks" class="check-list"></dl><div id="result-warnings"></div></section><noscript><p class="callout">JavaScript is required for local browser verification. The command-line verifier works without this page.</p></noscript><h2>Interpretation</h2><p>A passing result proves the checks displayed here. It does not certify the publisher or the truth of the signed statement.</p>`,
}));

const spec = await readFile(join(root, 'govp/spec/govp-1.txt'), 'utf8');
pages.set('govp/spec/govp-1.html', page({
  active: 'spec', title: 'The GOVP-1 Record Format', description: 'Normative specification for the stable GOVP-1 wire format and verification algorithm.',
  body: `<p><a class="btn btn--ghost" href="/govp/spec/govp-1.txt">Download normative plain text</a></p><div class="spec-source" role="document" aria-label="Normative GOVP-1 plain text"><pre>${escapeHtml(spec)}</pre></div>`,
}));

for (const [path, content] of pages) await writeFile(join(root, path), content, 'utf8');

await writeFile(join(root, 'robots.txt'), 'User-agent: *\nAllow: /\nSitemap: https://govp.io/sitemap.xml\n', 'utf8');

const sitemapPaths = Array.from(pages.keys())
  .filter((path) => path.endsWith('.html') && path !== '404.html')
  .map((path) => path === 'index.html' ? '' : path)
  .map((path) => `  <url><loc>https://govp.io/${path}</loc></url>`)
  .join('\n');
await writeFile(join(root, 'sitemap.xml'), `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${sitemapPaths}\n</urlset>\n`, 'utf8');

await writeFile(join(root, 'llms.txt'), `# GOVP\n\n> GOVP is the open protocol for portable, independently verifiable operational evidence.\n\nVersion: ${version}\nProtocol: GOVP-1 (stable)\nInstall: python -m pip install govp==${version}\nPyPI: ${pypiUrl}\nRelease: ${releaseUrl}\nCanonical source: https://github.com/govp-protocol/govp\nRelease source commit: ${sourceCommit}\nLicense: Apache-2.0\n\nPrimary documentation:\n- https://govp.io/govp/spec/govp-1.txt\n- https://govp.io/govp/docs.html\n- https://govp.io/govp/composition.html\n- https://govp.io/govp/conformance/vectors.json\n- https://govp.io/govp/conformance/json-vectors.json\n- https://govp.io/govp/schemas/govp-1.schema.json\n- https://govp.io/PROTOCOL-SOURCE.json\n- https://govp.io/.well-known/govp.txt\n\nInterpretation boundary: GOVP verifies record signature and integrity. When the asset is an upstream evidence object, GOVP binds its exact bytes but does not replace that system's native verification or policy.\n`, 'utf8');

console.log(`Built ${pages.size} HTML pages and discovery files.`);
