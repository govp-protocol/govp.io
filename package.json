{
  "name": "govp-io",
  "version": "0.1.11",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-site.mjs",
    "test": "node --test tests/*.test.mjs && node tools/check-site.mjs && node tools/check-protocol-assets.mjs",
    "check:deploy": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260804.1",
    "wrangler": "4.118.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
